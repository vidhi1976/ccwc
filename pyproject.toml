[project]
name = "ccwc"
version = "0.1.0"
description = "A Python implementation of the wc command using Typer"
readme = "README.md"
requires-python = ">=3.8"
dependencies = ["typer>=0.9.0"]

[project.scripts]
ccwc = "ccwc.main:app"
